
const filterConfig = {
  default: ["firstName", "lastName", "age", "sex", "address"],
  US: ["firstName", "lastName", "age", "sex"],
  EU: ["firstName", "age", "address"],
  ASIA: ["firstName", "lastName", "sex"]
};

export default filterConfig;
